[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fantasyleaguefootball"
version = "0.2.0"
description = "Draft-day board for Yahoo half-PPR fantasy football"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "SysAdminDoc", email = "matt_parker@outlook.com" }]
keywords = ["fantasy-football", "draft", "yahoo", "nfl"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Games/Entertainment",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.16", "playwright>=1.60"]

[project.scripts]
fantasyleague = "fantasyleague.cli:main"

[project.urls]
Repository = "https://github.com/SysAdminDoc/FantasyLeagueFootball"

[tool.hatch.build.targets.wheel]
# assets/ and data/ live inside the package, so they come along automatically.
packages = ["src/fantasyleague"]

[tool.ruff]
line-length = 110
src = ["src", "tests"]

[tool.ruff.lint]
# 0.16 enables a much larger default set; keep these on top of it.
extend-select = ["I", "UP", "B", "SIM", "RUF", "PTH", "C4", "PIE", "RET"]
# The probability docs deliberately use real math glyphs.
allowed-confusables = ["σ", "−", "·", "Φ"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
